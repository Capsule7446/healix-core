# application/engine 测试用例矩阵

## 范围与口径

本表记录 `application/engine` 的公开业务入口和全部顶层 Go testcase。Go 测试源码是唯一可执行事实；表驱动测试的全部子案例由其对应的测试函数统一引用。

## 公开 API 与用例入口

| 公开入口 | 定义文件 | 测试证据状态 |
|---|---|---|
| `CompilePlan` | [`application/engine/compiler.go`](../../application/engine/compiler.go) | 从完整的不可变 `execution.InstanceSnapshot` 编译 `CompiledPlan`；未封存快照返回 `EXECUTION_PLAN_UNSEALED`。 |
| `CompiledPlan.Entries()` / `CompiledPlan.Entry` | [`application/engine/compiler.go`](../../application/engine/compiler.go) | 以执行顺序返回调用方拥有的副本；按 `EntryID` 查找时不暴露私有索引或 `node.Program`。 |
| `RunProgram` | [`application/engine/engine.go`](../../application/engine/engine.go) | 校验 `InstanceID`、快照摘要、`EntryID` 与领取令牌，再通过 `ExecutionAuthorityVerifier` 验证权威，之后才访问运行端口。 |
| `Config` / `ExecutionAuthority` / `EntryResult` | [`application/engine/engine.go`](../../application/engine/engine.go) | 组合运行端口、执行权威和执行/录制/时间线结果；`ExecutionInterrupted` 等未知结果只由恢复流程使用。 |

## 测试用例证据矩阵

| 测试用例 | 输入、边界或业务前置状态 | 预期契约 | 可执行证据 |
|---|---|---|---|
| `TestCompilePlanRejectsSnapshotIdentityMatrix` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_matrix_test.go`](../../application/engine/compiler_matrix_test.go) · `TestCompilePlanRejectsSnapshotIdentityMatrix` |
| `TestCompilePlanRejectsDuplicateNestedStepIDs` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_matrix_test.go`](../../application/engine/compiler_matrix_test.go) · `TestCompilePlanRejectsDuplicateNestedStepIDs` |
| `TestCompilePlanBuildsWaitKinds` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_matrix_test.go`](../../application/engine/compiler_matrix_test.go) · `TestCompilePlanBuildsWaitKinds` |
| `TestCompilePlanBuildsCompleteStepTreeWithoutAliasingPlan` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_matrix_test.go`](../../application/engine/compiler_matrix_test.go) · `TestCompilePlanBuildsCompleteStepTreeWithoutAliasingPlan` |
| `TestCompilePlanPreservesCompleteFingerprint` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_matrix_test.go`](../../application/engine/compiler_matrix_test.go) · `TestCompilePlanPreservesCompleteFingerprint` |
| `TestCompilePlanIndexesInvocationsOnceAcrossEntries` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_test.go`](../../application/engine/compiler_test.go) · `TestCompilePlanIndexesInvocationsOnceAcrossEntries` |
| `TestCompilePlanInjectsEnvironmentIntoParameterlessRoot` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_test.go`](../../application/engine/compiler_test.go) · `TestCompilePlanInjectsEnvironmentIntoParameterlessRoot` |
| `TestCompilePlanRejectsUnsealedZeroValue` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_test.go`](../../application/engine/compiler_test.go) · `TestCompilePlanRejectsUnsealedZeroValue` |
| `TestCompilerRequiresConcreteRootAndNestedInvocations` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_test.go`](../../application/engine/compiler_test.go) · `TestCompilerRequiresConcreteRootAndNestedInvocations` |
| `TestCompilePlanRejectsUnsealedPlanWithDomainError` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_test.go`](../../application/engine/compiler_test.go) · `TestCompilePlanRejectsUnsealedPlanWithDomainError` |
| `TestRuntimeFlowFragmentStepIDIsCollisionFree` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_test.go`](../../application/engine/compiler_test.go) · `TestRuntimeFlowFragmentStepIDIsCollisionFree` |
| `TestCompilePlanBuildsLockedWorkflowTreeAndBindsChildDefaults` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_test.go`](../../application/engine/compiler_test.go) · `TestCompilePlanBuildsLockedWorkflowTreeAndBindsChildDefaults` |
| `TestCompilePlanCreatesDistinctRuntimeIdentitiesForSharedChildInvocations` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_test.go`](../../application/engine/compiler_test.go) · `TestCompilePlanCreatesDistinctRuntimeIdentitiesForSharedChildInvocations` |
| `TestCompilePlanKeepsTwoVersionsOfSameNodeExact` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_test.go`](../../application/engine/compiler_test.go) · `TestCompilePlanKeepsTwoVersionsOfSameNodeExact` |
| `TestCompilePlanBuildsValidationGroup` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_test.go`](../../application/engine/compiler_test.go) · `TestCompilePlanBuildsValidationGroup` |
| `TestCompilePlanRejectsMissingSnapshotAndCycles` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_test.go`](../../application/engine/compiler_test.go) · `TestCompilePlanRejectsMissingSnapshotAndCycles` |
| `TestCompilePlanKeepsRepeatedEntryOccurrencesIndependent` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_test.go`](../../application/engine/compiler_test.go) · `TestCompilePlanKeepsRepeatedEntryOccurrencesIndependent` |
| `TestCompilePlanInjectsTypedEnvironmentValues` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_test.go`](../../application/engine/compiler_test.go) · `TestCompilePlanInjectsTypedEnvironmentValues` |
| `TestRunProgramCreatesAnExecutionLocalRuntime` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/engine_contract_matrix_test.go`](../../application/engine/engine_contract_matrix_test.go) · `TestRunProgramCreatesAnExecutionLocalRuntime` |
| `TestRunProgramReturnsRecorderStopFailureAfterSuccessfulRoot` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/engine_contract_matrix_test.go`](../../application/engine/engine_contract_matrix_test.go) · `TestRunProgramReturnsRecorderStopFailureAfterSuccessfulRoot` |
| `TestRunProgramPropagatesStepIntervalToRuntime` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/engine_test.go`](../../application/engine/engine_test.go) · `TestRunProgramPropagatesStepIntervalToRuntime` |
| `TestRunProgramRetainsSuccessfulRecording` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/engine_test.go`](../../application/engine/engine_test.go) · `TestRunProgramRetainsSuccessfulRecording` |
| `TestRunProgramRejectsIncompleteConfigurationBeforeExecution` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/engine_test.go`](../../application/engine/engine_test.go) · `TestRunProgramRejectsIncompleteConfigurationBeforeExecution` |
| `TestRunProgramRecorderFailureAndDetachedCleanupContract` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/engine_test.go`](../../application/engine/engine_test.go) · `TestRunProgramRecorderFailureAndDetachedCleanupContract` |
| `TestCompilePlanMapsNestedValidationAndReferences` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/plan_mapper_test.go`](../../application/engine/plan_mapper_test.go) · `TestCompilePlanMapsNestedValidationAndReferences` |
| `TestCompilePlanRequiresInstanceIdentity` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/plan_mapper_test.go`](../../application/engine/plan_mapper_test.go) · `TestCompilePlanRequiresInstanceIdentity` |
| `TestRunProgramReportsTimelineStartFailureBeforeLeafExecution` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/result_test.go`](../../application/engine/result_test.go) · `TestRunProgramReportsTimelineStartFailureBeforeLeafExecution` |
| `TestRunProgramReportsFailureWhenLaterLeafTimelineStartFails` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/result_test.go`](../../application/engine/result_test.go) · `TestRunProgramReportsFailureWhenLaterLeafTimelineStartFails` |
| `TestRunProgramKeepsExecutionSuccessWhenTimelineFinishFails` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/result_test.go`](../../application/engine/result_test.go) · `TestRunProgramKeepsExecutionSuccessWhenTimelineFinishFails` |
| `TestRunProgramRejectsTimelineWithoutRecorder` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/result_test.go`](../../application/engine/result_test.go) · `TestRunProgramRejectsTimelineWithoutRecorder` |
| `TestExecutionOutcomePreservesSuccess` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/result_test.go`](../../application/engine/result_test.go) · `TestExecutionOutcomePreservesSuccess` |
| `TestExecutionOutcomePreservesBusinessFailure` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/result_test.go`](../../application/engine/result_test.go) · `TestExecutionOutcomePreservesBusinessFailure` |
| `TestRunProgramReportsTimelineStartFailureWhenRecorderStartFails` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/result_test.go`](../../application/engine/result_test.go) · `TestRunProgramReportsTimelineStartFailureWhenRecorderStartFails` |
| `TestRunProgramAllowsEmptyCompletionChainWithoutBrowser` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/result_test.go`](../../application/engine/result_test.go) · `TestRunProgramAllowsEmptyCompletionChainWithoutBrowser` |
| `TestRunProgramClassifiesMissingCompletionBrowser` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/result_test.go`](../../application/engine/result_test.go) · `TestRunProgramClassifiesMissingCompletionBrowser` |
| `TestRunProgramReportsObserverFailureWithoutChangingExecutionOutcome` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/result_test.go`](../../application/engine/result_test.go) · `TestRunProgramReportsObserverFailureWithoutChangingExecutionOutcome` |
| `TestRunProgramClassifiesEveryContextTerminationAsCanceled` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/result_test.go`](../../application/engine/result_test.go) · `TestRunProgramClassifiesEveryContextTerminationAsCanceled` |
| `TestRunProgramRejectsNilRecorderTimelineBeforeExecution` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/result_test.go`](../../application/engine/result_test.go) · `TestRunProgramRejectsNilRecorderTimelineBeforeExecution` |
| `TestCompileSnapshotDraftRejectsDuplicatePublicIdentities` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/strict_boundary_test.go`](../../application/engine/strict_boundary_test.go) · `TestCompileSnapshotDraftRejectsDuplicatePublicIdentities` |
| `TestRunProgramRejectsNilContextAndNegativeInterval` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/strict_boundary_test.go`](../../application/engine/strict_boundary_test.go) · `TestRunProgramRejectsNilContextAndNegativeInterval` |
| `TestCompilerRejectsNegativeWaitMilliseconds` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/strict_boundary_test.go`](../../application/engine/strict_boundary_test.go) · `TestCompilerRejectsNegativeWaitMilliseconds` |
| `TestMillisecondsDurationRejectsOnlyInvalidInt64Boundaries` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/strict_boundary_test.go`](../../application/engine/strict_boundary_test.go) · `TestMillisecondsDurationRejectsOnlyInvalidInt64Boundaries` |
| `TestCompiledPlanAccessorsReturnIndependentEntries` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiled_run_ownership_test.go`](../../application/engine/compiled_run_ownership_test.go) · `TestCompiledPlanAccessorsReturnIndependentEntries` |
| `TestCompiledPlanEntryRejectsCorruptedIndexedIdentity` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiled_run_ownership_test.go`](../../application/engine/compiled_run_ownership_test.go) · `TestCompiledPlanEntryRejectsCorruptedIndexedIdentity` |
| `TestRunProgramRejectsExecutionIdentityMismatchWithoutSideEffects` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/execution_identity_test.go`](../../application/engine/execution_identity_test.go) · `TestRunProgramRejectsExecutionIdentityMismatchWithoutSideEffects` |
| `TestRunProgramRequiresCurrentExecutionAuthorityBeforeSideEffects` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/execution_identity_test.go`](../../application/engine/execution_identity_test.go) · `TestRunProgramRequiresCurrentExecutionAuthorityBeforeSideEffects` |
| `TestRunProgramForwardsCompleteExecutionAuthority` | 测试函数覆盖源码声明的输入、状态与边界；表驱动子案例由该函数统一维护。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/execution_identity_test.go`](../../application/engine/execution_identity_test.go) · `TestRunProgramForwardsCompleteExecutionAuthority` |
| `TestCompilePlanStampsEveryStepMetadataWithItsCallSite` | 根流程并列调用同一子流程两次，检查编译出的每条 `StepMetadata` 的 `InvocationPath`。 | 无一为零值；两次调用的子步骤坐标互不相同且都不等于 entry 根路径；调用步骤自身留在父作用域，被调用的 workflow 进入新作用域。 | [`application/engine/compiler_invocation_path_test.go`](../../application/engine/compiler_invocation_path_test.go) · `TestCompilePlanStampsEveryStepMetadataWithItsCallSite` |
| `TestRunProgramRefusesHealAcrossAnOriginBoundary` | 经真实 `CompilePlan → RunProgram`：目标录制于 `trusted.test`，首次定位失败，healer 给出 0.99 分候选，实时页面分别为 `evil.test`、端口报错、位置为空。 | 三种情形都以 `EXECUTION_HEALING_REFUSED` 拒绝，click/input 与 healed selector 解析次数均为 0，私有 cause 携带 `origin_mismatch`/`origin_unknown`，暂存决策为 `SafetyRejected`（区别于 Review），公开文案不含页面、选择器或原因。 | [`application/engine/heal_origin_authority_test.go`](../../application/engine/heal_origin_authority_test.go) · `TestRunProgramRefusesHealAcrossAnOriginBoundary` |
| `TestRunProgramAllowsHealOnTheRecordedOrigin` | 同一装配，实时页面即录制时的 `trusted.test`。 | 自愈放行且被点击一次——fail-closed 规则若连合法情形也拒绝，就只是一次故障。 | [`application/engine/heal_origin_authority_test.go`](../../application/engine/heal_origin_authority_test.go) · `TestRunProgramAllowsHealOnTheRecordedOrigin` |
| `TestRunProgramRejectsHealingWithoutALocationPort` | 启用 Healer 但不提供 `PageLocator`。 | 在访问运行端口阶段即返回 `EXECUTION_RUNTIME_CONFIGURATION_INVALID`——配置错误不能静默表现为自愈无效。 | [`application/engine/heal_origin_authority_test.go`](../../application/engine/heal_origin_authority_test.go) · `TestRunProgramRejectsHealingWithoutALocationPort` |
| `TestRunProgramClassifiesBareFailures` | 根节点返回 `errors.New`、`fmt.Errorf(%w)`、`errors.Join`、以及文本含 token 与内网 URL 的裸 error。 | 一律包装为 `EXECUTION_OPERATION_FAILED`/`INTERNAL`，原 error 经 `Unwrap` 仍可达，公开文案不回显 cause 与宿主细节。 | [`application/engine/unclassified_failure_test.go`](../../application/engine/unclassified_failure_test.go) · `TestRunProgramClassifiesBareFailures` |
| `TestRunProgramPassesClassifiedFailuresThroughUnchanged` | 根节点返回已分类 fault：element not found、healing refused、worker fence stale。 | code 与 kind 原样透出且对象同一——二次包装会把宿主用来分支的具体码换成通用码，比不分类更糟。 | [`application/engine/unclassified_failure_test.go`](../../application/engine/unclassified_failure_test.go) · `TestRunProgramPassesClassifiedFailuresThroughUnchanged` |
| `TestRunProgramReturnsNoErrorOnSuccess` | 根节点成功返回。 | 无 error 且 outcome 为 SUCCEEDED——钉住 nil 分支，防止分类器凭空造出 fault。 | [`application/engine/unclassified_failure_test.go`](../../application/engine/unclassified_failure_test.go) · `TestRunProgramReturnsNoErrorOnSuccess` |
| `TestRunProgramKeepsCancellationDistinguishable` | 根节点返回 `context.Canceled` 与 `context.DeadlineExceeded`。 | `errors.Is` 仍成立且 outcome 为 CANCELED——朴素的分类器会把取消变成 INTERNAL 操作失败，宿主随后重试一件操作员刻意停掉的事。 | [`application/engine/unclassified_failure_test.go`](../../application/engine/unclassified_failure_test.go) · `TestRunProgramKeepsCancellationDistinguishable` |

## 跨入口与一致性用例

同包及其子目录中名称含 `Conformance`、`Transaction`、`Race`、`Rollback`、`Replay`、`Concurrent` 或 `Fence` 的测试，属于跨入口契约；它们已在上方矩阵逐行列出。application 包的 `conformancetest/` 证据也归属此表。

## 维护规则

1. 新增或删除 `Test…` 函数时，必须同步更新本表；表驱动新增子案例要更新相应行的边界描述。
2. 新增公开 domain API 或 application use case 时，必须先添加公开入口清单行和至少一条可执行测试证据。
3. 文档不替代测试；冲突时以 Go 测试断言和领域契约为准。
