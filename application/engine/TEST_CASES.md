# application/engine Test Case Matrix

## 范围与口径

本表记录 `application/engine` 的公开业务入口和全部顶层 Go testcase。Go 测试源码是唯一可执行事实；表驱动测试的全部子案例由其对应的测试函数统一引用。

## Public API / Use-case Inventory

| 公开入口 | 定义文件 | 测试证据状态 |
|---|---|---|
| `CompiledRun.Entry` | [`application/engine/compiler.go`](../../application/engine/compiler.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `CompileRunSnapshot` | [`application/engine/compiler.go`](../../application/engine/compiler.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `RunCoordinator.Run` | [`application/engine/coordinator.go`](../../application/engine/coordinator.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `RunCompiledEntry` | [`application/engine/engine.go`](../../application/engine/engine.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `RunCompiledEntryWithResult` | [`application/engine/engine.go`](../../application/engine/engine.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |

## Test Case Evidence Matrix

| Test case | 输入、边界或业务前置状态 | 预期契约 | 可执行证据 |
|---|---|---|---|
| `TestCompilePlanRejectsSnapshotIdentityMatrix` | `Compile Plan Rejects Snapshot Identity Matrix`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_matrix_test.go`](../../application/engine/compiler_matrix_test.go) · `TestCompilePlanRejectsSnapshotIdentityMatrix` |
| `TestCompilePlanRejectsDuplicateNestedStepIDs` | `Compile Plan Rejects Duplicate Nested Step IDs`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_matrix_test.go`](../../application/engine/compiler_matrix_test.go) · `TestCompilePlanRejectsDuplicateNestedStepIDs` |
| `TestCompilePlanBuildsWaitKinds` | `Compile Plan Builds Wait Kinds`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_matrix_test.go`](../../application/engine/compiler_matrix_test.go) · `TestCompilePlanBuildsWaitKinds` |
| `TestCompilePlanBuildsCompleteStepTreeWithoutAliasingPlan` | `Compile Plan Builds Complete Step Tree Without Aliasing Plan`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_matrix_test.go`](../../application/engine/compiler_matrix_test.go) · `TestCompilePlanBuildsCompleteStepTreeWithoutAliasingPlan` |
| `TestCompilePlanPreservesCompleteFingerprint` | `Compile Plan Preserves Complete Fingerprint`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_matrix_test.go`](../../application/engine/compiler_matrix_test.go) · `TestCompilePlanPreservesCompleteFingerprint` |
| `TestCompiledRunEntryDirectBoundaryAndCorruptIndexStates` | `Compiled Run Entry Direct Boundary And Corrupt Index States`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_test.go`](../../application/engine/compiler_test.go) · `TestCompiledRunEntryDirectBoundaryAndCorruptIndexStates` |
| `TestCompileRunSnapshotDirectMinimalPublicCase` | `Compile Run Snapshot Direct Minimal Public Case`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_test.go`](../../application/engine/compiler_test.go) · `TestCompileRunSnapshotDirectMinimalPublicCase` |
| `TestCompileRunSnapshotIndexesInvocationsOnceAcrossEntries` | `Compile Run Snapshot Indexes Invocations Once Across Entries`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_test.go`](../../application/engine/compiler_test.go) · `TestCompileRunSnapshotIndexesInvocationsOnceAcrossEntries` |
| `TestCompileRunSnapshotInjectsEnvironmentIntoParameterlessRoot` | `Compile Run Snapshot Injects Environment Into Parameterless Root`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_test.go`](../../application/engine/compiler_test.go) · `TestCompileRunSnapshotInjectsEnvironmentIntoParameterlessRoot` |
| `TestCompileRunSnapshotRejectsUnsealedZeroValue` | `Compile Run Snapshot Rejects Unsealed Zero Value`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_test.go`](../../application/engine/compiler_test.go) · `TestCompileRunSnapshotRejectsUnsealedZeroValue` |
| `TestCompilerRequiresConcreteRootAndNestedInvocations` | `Compiler Requires Concrete Root And Nested Invocations`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_test.go`](../../application/engine/compiler_test.go) · `TestCompilerRequiresConcreteRootAndNestedInvocations` |
| `TestCompilePlanRejectsUnsealedPlanWithDomainError` | `Compile Plan Rejects Unsealed Plan With Domain Error`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_test.go`](../../application/engine/compiler_test.go) · `TestCompilePlanRejectsUnsealedPlanWithDomainError` |
| `TestRuntimeWorkflowStepIDIsCollisionFree` | `Runtime Workflow Step IDIs Collision Free`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_test.go`](../../application/engine/compiler_test.go) · `TestRuntimeWorkflowStepIDIsCollisionFree` |
| `TestCompilePlanBuildsLockedWorkflowTreeAndBindsChildDefaults` | `Compile Plan Builds Locked Workflow Tree And Binds Child Defaults`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_test.go`](../../application/engine/compiler_test.go) · `TestCompilePlanBuildsLockedWorkflowTreeAndBindsChildDefaults` |
| `TestCompilePlanCreatesDistinctRuntimeIdentitiesForSharedChildInvocations` | `Compile Plan Creates Distinct Runtime Identities For Shared Child Invocations`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_test.go`](../../application/engine/compiler_test.go) · `TestCompilePlanCreatesDistinctRuntimeIdentitiesForSharedChildInvocations` |
| `TestCompilePlanKeepsTwoVersionsOfSameNodeExact` | `Compile Plan Keeps Two Versions Of Same Node Exact`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_test.go`](../../application/engine/compiler_test.go) · `TestCompilePlanKeepsTwoVersionsOfSameNodeExact` |
| `TestCompilePlanBuildsValidationGroup` | `Compile Plan Builds Validation Group`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_test.go`](../../application/engine/compiler_test.go) · `TestCompilePlanBuildsValidationGroup` |
| `TestCompilePlanRejectsMissingSnapshotAndCycles` | `Compile Plan Rejects Missing Snapshot And Cycles`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_test.go`](../../application/engine/compiler_test.go) · `TestCompilePlanRejectsMissingSnapshotAndCycles` |
| `TestCompilePlanKeepsRepeatedEntryOccurrencesIndependent` | `Compile Plan Keeps Repeated Entry Occurrences Independent`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/compiler_test.go`](../../application/engine/compiler_test.go) · `TestCompilePlanKeepsRepeatedEntryOccurrencesIndependent` |
| `TestRunCompiledEntryCreatesAnExecutionLocalRuntime` | `Run Compiled Entry Creates An Execution Local Runtime`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/engine_contract_matrix_test.go`](../../application/engine/engine_contract_matrix_test.go) · `TestRunCompiledEntryCreatesAnExecutionLocalRuntime` |
| `TestRunCompiledEntryReturnsRecorderStopFailureAfterSuccessfulRoot` | `Run Compiled Entry Returns Recorder Stop Failure After Successful Root`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/engine_contract_matrix_test.go`](../../application/engine/engine_contract_matrix_test.go) · `TestRunCompiledEntryReturnsRecorderStopFailureAfterSuccessfulRoot` |
| `TestRunCompiledEntryPropagatesStepIntervalToRuntime` | `Run Compiled Entry Propagates Step Interval To Runtime`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/engine_test.go`](../../application/engine/engine_test.go) · `TestRunCompiledEntryPropagatesStepIntervalToRuntime` |
| `TestRunCompiledEntryRetainsSuccessfulRecording` | `Run Compiled Entry Retains Successful Recording`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/engine_test.go`](../../application/engine/engine_test.go) · `TestRunCompiledEntryRetainsSuccessfulRecording` |
| `TestRunCompiledEntryRejectsIncompleteConfigurationBeforeExecution` | `Run Compiled Entry Rejects Incomplete Configuration Before Execution`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/engine_test.go`](../../application/engine/engine_test.go) · `TestRunCompiledEntryRejectsIncompleteConfigurationBeforeExecution` |
| `TestRunCompiledEntryRecorderFailureAndDetachedCleanupContract` | `Run Compiled Entry Recorder Failure And Detached Cleanup Contract`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/engine_test.go`](../../application/engine/engine_test.go) · `TestRunCompiledEntryRecorderFailureAndDetachedCleanupContract` |
| `TestRunCompiledEntryDirectBoundaryCases` | `Run Compiled Entry Direct Boundary Cases`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/engine_test.go`](../../application/engine/engine_test.go) · `TestRunCompiledEntryDirectBoundaryCases` |
| `TestRunCompiledEntryWithResultDirectClassificationAndRecorderCases` | `Run Compiled Entry With Result Direct Classification And Recorder Cases`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/engine_test.go`](../../application/engine/engine_test.go) · `TestRunCompiledEntryWithResultDirectClassificationAndRecorderCases` |
| `TestRunCoordinatorRunDirectInvalidBoundaries` | `Run Coordinator Run Direct Invalid Boundaries`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/engine_test.go`](../../application/engine/engine_test.go) · `TestRunCoordinatorRunDirectInvalidBoundaries` |
| `TestRunCoordinatorRunIsolatesRuntimeAndForwardsExactConfig` | `Run Coordinator Run Isolates Runtime And Forwards Exact Config`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/engine_test.go`](../../application/engine/engine_test.go) · `TestRunCoordinatorRunIsolatesRuntimeAndForwardsExactConfig` |
| `TestCompilePlanMapsNestedValidationAndReferences` | `Compile Plan Maps Nested Validation And References`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/plan_mapper_test.go`](../../application/engine/plan_mapper_test.go) · `TestCompilePlanMapsNestedValidationAndReferences` |
| `TestCompilePlanRequiresRunIdentity` | `Compile Plan Requires Run Identity`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/plan_mapper_test.go`](../../application/engine/plan_mapper_test.go) · `TestCompilePlanRequiresRunIdentity` |
| `TestRunCompiledEntryWithResultReportsTimelineStartFailureBeforeLeafExecution` | `Run Compiled Entry With Result Reports Timeline Start Failure Before Leaf Execution`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/result_test.go`](../../application/engine/result_test.go) · `TestRunCompiledEntryWithResultReportsTimelineStartFailureBeforeLeafExecution` |
| `TestRunCompiledEntryWithResultReportsFailureWhenLaterLeafTimelineStartFails` | `Run Compiled Entry With Result Reports Failure When Later Leaf Timeline Start Fails`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/result_test.go`](../../application/engine/result_test.go) · `TestRunCompiledEntryWithResultReportsFailureWhenLaterLeafTimelineStartFails` |
| `TestRunCompiledEntryWithResultKeepsExecutionSuccessWhenTimelineFinishFails` | `Run Compiled Entry With Result Keeps Execution Success When Timeline Finish Fails`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/result_test.go`](../../application/engine/result_test.go) · `TestRunCompiledEntryWithResultKeepsExecutionSuccessWhenTimelineFinishFails` |
| `TestRunCompiledEntryWithResultRejectsTimelineWithoutRecorder` | `Run Compiled Entry With Result Rejects Timeline Without Recorder`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/result_test.go`](../../application/engine/result_test.go) · `TestRunCompiledEntryWithResultRejectsTimelineWithoutRecorder` |
| `TestExecutionOutcomePreservesSuccess` | `Execution Outcome Preserves Success`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/result_test.go`](../../application/engine/result_test.go) · `TestExecutionOutcomePreservesSuccess` |
| `TestExecutionOutcomePreservesBusinessFailure` | `Execution Outcome Preserves Business Failure`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/result_test.go`](../../application/engine/result_test.go) · `TestExecutionOutcomePreservesBusinessFailure` |
| `TestRunCompiledEntryWithResultReportsTimelineStartFailureWhenRecorderStartFails` | `Run Compiled Entry With Result Reports Timeline Start Failure When Recorder Start Fails`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/result_test.go`](../../application/engine/result_test.go) · `TestRunCompiledEntryWithResultReportsTimelineStartFailureWhenRecorderStartFails` |
| `TestRunCompiledEntryWithResultAllowsEmptyCompletionChainWithoutBrowser` | `Run Compiled Entry With Result Allows Empty Completion Chain Without Browser`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/result_test.go`](../../application/engine/result_test.go) · `TestRunCompiledEntryWithResultAllowsEmptyCompletionChainWithoutBrowser` |
| `TestRunCompiledEntryWithResultClassifiesMissingCompletionBrowser` | `Run Compiled Entry With Result Classifies Missing Completion Browser`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/result_test.go`](../../application/engine/result_test.go) · `TestRunCompiledEntryWithResultClassifiesMissingCompletionBrowser` |
| `TestRunCompiledEntryWithResultReportsObserverFailureWithoutChangingExecutionOutcome` | `Run Compiled Entry With Result Reports Observer Failure Without Changing Execution Outcome`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/result_test.go`](../../application/engine/result_test.go) · `TestRunCompiledEntryWithResultReportsObserverFailureWithoutChangingExecutionOutcome` |
| `TestCompileSnapshotDraftRejectsDuplicatePublicIdentities` | `Compile Snapshot Draft Rejects Duplicate Public Identities`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/strict_boundary_test.go`](../../application/engine/strict_boundary_test.go) · `TestCompileSnapshotDraftRejectsDuplicatePublicIdentities` |
| `TestRunCompiledEntryRejectsNilContextAndNegativeInterval` | `Run Compiled Entry Rejects Nil Context And Negative Interval`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/strict_boundary_test.go`](../../application/engine/strict_boundary_test.go) · `TestRunCompiledEntryRejectsNilContextAndNegativeInterval` |
| `TestCompilerRejectsMillisecondDurationOverflowWithoutAllocating` | `Compiler Rejects Millisecond Duration Overflow Without Allocating`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`application/engine/strict_boundary_test.go`](../../application/engine/strict_boundary_test.go) · `TestCompilerRejectsMillisecondDurationOverflowWithoutAllocating` |

## Cross-cutting / Conformance Cases

同包及其子目录中名称含 `Conformance`、`Transaction`、`Race`、`Rollback`、`Replay`、`Concurrent` 或 `Fence` 的测试，属于跨入口契约；它们已在上方矩阵逐行列出。application 包的 `conformancetest/` 证据也归属此表。

## 维护规则

1. 新增或删除 `Test…` 函数时，必须同步更新本表；表驱动新增子案例要更新相应行的边界描述。
2. 新增公开 domain API 或 application use case 时，必须先添加公开入口清单行和至少一条可执行测试证据。
3. 文档不替代测试；冲突时以 Go 测试断言和领域契约为准。
