# domain/execution Test Case Matrix

## 范围与口径

本表记录 `domain/execution` 的公开业务入口和全部顶层 Go testcase。Go 测试源码是唯一可执行事实；表驱动测试的全部子案例由其对应的测试函数统一引用。

## Public API / Use-case Inventory

| 公开入口 | 定义文件 | 测试证据状态 |
|---|---|---|
| `FailurePolicy.IsValid` | [`domain/execution/plan.go`](../../domain/execution/plan.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `Seal` | [`domain/execution/plan.go`](../../domain/execution/plan.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `Plan.IsSealed` | [`domain/execution/plan.go`](../../domain/execution/plan.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `Plan.Validate` | [`domain/execution/plan.go`](../../domain/execution/plan.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `Plan.Snapshot` | [`domain/execution/plan.go`](../../domain/execution/plan.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `Plan.RunID` | [`domain/execution/plan.go`](../../domain/execution/plan.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `Plan.FailurePolicy` | [`domain/execution/plan.go`](../../domain/execution/plan.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `Plan.Entries` | [`domain/execution/plan.go`](../../domain/execution/plan.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `Plan.Workflows` | [`domain/execution/plan.go`](../../domain/execution/plan.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `Plan.Nodes` | [`domain/execution/plan.go`](../../domain/execution/plan.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `Plan.References` | [`domain/execution/plan.go`](../../domain/execution/plan.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `ValidateRunStatusTransition` | [`domain/execution/run.go`](../../domain/execution/run.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `NewRun` | [`domain/execution/run.go`](../../domain/execution/run.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `HydrateRun` | [`domain/execution/run.go`](../../domain/execution/run.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `ValidateRun` | [`domain/execution/run.go`](../../domain/execution/run.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `Run.Transition` | [`domain/execution/run.go`](../../domain/execution/run.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `DefaultHealerPolicySnapshot` | [`domain/execution/run_snapshot.go`](../../domain/execution/run_snapshot.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `RunSnapshot.Digest` | [`domain/execution/run_snapshot.go`](../../domain/execution/run_snapshot.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `RunSnapshot.SchemaVersion` | [`domain/execution/run_snapshot.go`](../../domain/execution/run_snapshot.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `RunSnapshot.RunID` | [`domain/execution/run_snapshot.go`](../../domain/execution/run_snapshot.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `RunSnapshot.TestTaskID` | [`domain/execution/run_snapshot.go`](../../domain/execution/run_snapshot.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `RunSnapshot.TestTaskVersionID` | [`domain/execution/run_snapshot.go`](../../domain/execution/run_snapshot.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `RunSnapshot.Input` | [`domain/execution/run_snapshot.go`](../../domain/execution/run_snapshot.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `RunSnapshot.Plan` | [`domain/execution/run_snapshot.go`](../../domain/execution/run_snapshot.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `RunSnapshot.Invocations` | [`domain/execution/run_snapshot.go`](../../domain/execution/run_snapshot.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `RunSnapshot.Invocation` | [`domain/execution/run_snapshot.go`](../../domain/execution/run_snapshot.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `RunSnapshot.Environment` | [`domain/execution/run_snapshot.go`](../../domain/execution/run_snapshot.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `SealRunSnapshot` | [`domain/execution/run_snapshot.go`](../../domain/execution/run_snapshot.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `HydrateRunSnapshot` | [`domain/execution/run_snapshot.go`](../../domain/execution/run_snapshot.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `ValidateExecutionStatusTransition` | [`domain/execution/status.go`](../../domain/execution/status.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `IsTerminalExecutionStatus` | [`domain/execution/status.go`](../../domain/execution/status.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `ExecutionStatus.CanTransitionTo` | [`domain/execution/status.go`](../../domain/execution/status.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `Parameter.Validate` | [`domain/execution/validation.go`](../../domain/execution/validation.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `Draft.Validate` | [`domain/execution/validation.go`](../../domain/execution/validation.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `WorkflowSnapshot.Validate` | [`domain/execution/validation.go`](../../domain/execution/validation.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `Reference.Validate` | [`domain/execution/validation.go`](../../domain/execution/validation.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `Validation.Validate` | [`domain/execution/validation.go`](../../domain/execution/validation.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `ValidationGroup.Validate` | [`domain/execution/validation.go`](../../domain/execution/validation.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `WorkerFence.Validate` | [`domain/execution/worker_fence.go`](../../domain/execution/worker_fence.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `StaleWorkerFenceError.Error` | [`domain/execution/worker_fence.go`](../../domain/execution/worker_fence.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `StaleWorkerFenceError.Is` | [`domain/execution/worker_fence.go`](../../domain/execution/worker_fence.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |

## Test Case Evidence Matrix

| Test case | 输入、边界或业务前置状态 | 预期契约 | 可执行证据 |
|---|---|---|---|
| `TestFailurePolicyAndPlanFailurePolicyDirect` | `Failure Policy And Plan Failure Policy Direct`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/direct_gap_test.go`](../../domain/execution/direct_gap_test.go) · `TestFailurePolicyAndPlanFailurePolicyDirect` |
| `TestValidateRunDirect` | `Validate Run Direct`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/direct_gap_test.go`](../../domain/execution/direct_gap_test.go) · `TestValidateRunDirect` |
| `TestRunSnapshotNamedAccessorsAndInvocationIsolationDirect` | `Run Snapshot Named Accessors And Invocation Isolation Direct`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/direct_gap_test.go`](../../domain/execution/direct_gap_test.go) · `TestRunSnapshotNamedAccessorsAndInvocationIsolationDirect` |
| `TestExecutionStatusDirect` | `Execution Status Direct`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/direct_gap_test.go`](../../domain/execution/direct_gap_test.go) · `TestExecutionStatusDirect` |
| `TestInvocationEdgeKeyDoesNotCollideLikeDelimitedStrings` | `Invocation Edge Key Does Not Collide Like Delimited Strings`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/invocation_edge_test.go`](../../domain/execution/invocation_edge_test.go) · `TestInvocationEdgeKeyDoesNotCollideLikeDelimitedStrings` |
| `TestRunSnapshotRequiresCanonicalChildInvocationPath` | `Run Snapshot Requires Canonical Child Invocation Path`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/invocation_edge_test.go`](../../domain/execution/invocation_edge_test.go) · `TestRunSnapshotRequiresCanonicalChildInvocationPath` |
| `TestRunSnapshotUsesConcreteParentPathForRepeatedReferenceEdges` | `Run Snapshot Uses Concrete Parent Path For Repeated Reference Edges`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/invocation_edge_test.go`](../../domain/execution/invocation_edge_test.go) · `TestRunSnapshotUsesConcreteParentPathForRepeatedReferenceEdges` |
| `TestRunSnapshotRequiresCompleteConcreteBindingsAndChildValues` | `Run Snapshot Requires Complete Concrete Bindings And Child Values`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/invocation_edge_test.go`](../../domain/execution/invocation_edge_test.go) · `TestRunSnapshotRequiresCompleteConcreteBindingsAndChildValues` |
| `TestRunSnapshotPermitsConcreteBindingDifferentFromAuthoringMetadata` | `Run Snapshot Permits Concrete Binding Different From Authoring Metadata`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/invocation_edge_test.go`](../../domain/execution/invocation_edge_test.go) · `TestRunSnapshotPermitsConcreteBindingDifferentFromAuthoringMetadata` |
| `TestSealedNavigationURLRejectsAuthorityInterpolation` | `Sealed Navigation URLRejects Authority Interpolation`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/navigation_security_test.go`](../../domain/execution/navigation_security_test.go) · `TestSealedNavigationURLRejectsAuthorityInterpolation` |
| `TestSealedNavigationURLAllowsPathAndQueryInterpolation` | `Sealed Navigation URLAllows Path And Query Interpolation`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/navigation_security_test.go`](../../domain/execution/navigation_security_test.go) · `TestSealedNavigationURLAllowsPathAndQueryInterpolation` |
| `TestSealIndependentlyRejectsInvalidParameterDefinitionsAndValues` | `Seal Independently Rejects Invalid Parameter Definitions And Values`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/parameter_validation_test.go`](../../domain/execution/parameter_validation_test.go) · `TestSealIndependentlyRejectsInvalidParameterDefinitionsAndValues` |
| `TestSealRejectsInvalidSnapshotForParameterlessWorkflow` | `Seal Rejects Invalid Snapshot For Parameterless Workflow`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/parameter_validation_test.go`](../../domain/execution/parameter_validation_test.go) · `TestSealRejectsInvalidSnapshotForParameterlessWorkflow` |
| `TestSealRejectsMismatchedParameterizedSnapshotIdentity` | `Seal Rejects Mismatched Parameterized Snapshot Identity`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/parameter_validation_test.go`](../../domain/execution/parameter_validation_test.go) · `TestSealRejectsMismatchedParameterizedSnapshotIdentity` |
| `TestSealAccountsForParameterOptionCollectionElements` | `Seal Accounts For Parameter Option Collection Elements`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/parameter_validation_test.go`](../../domain/execution/parameter_validation_test.go) · `TestSealAccountsForParameterOptionCollectionElements` |
| `TestSealAccountsForTypedSnapshotAndMultiSelectResources` | `Seal Accounts For Typed Snapshot And Multi Select Resources`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/parameter_validation_test.go`](../../domain/execution/parameter_validation_test.go) · `TestSealAccountsForTypedSnapshotAndMultiSelectResources` |
| `TestSealRunSnapshotRejectsBindingsOnRootInvocation` | `Seal Run Snapshot Rejects Bindings On Root Invocation`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_snapshot_corrections_test.go`](../../domain/execution/run_snapshot_corrections_test.go) · `TestSealRunSnapshotRejectsBindingsOnRootInvocation` |
| `TestRunSnapshotFreezesCompleteExecutionPlanAndInvocationScopes` | `Run Snapshot Freezes Complete Execution Plan And Invocation Scopes`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_snapshot_corrections_test.go`](../../domain/execution/run_snapshot_corrections_test.go) · `TestRunSnapshotFreezesCompleteExecutionPlanAndInvocationScopes` |
| `TestRunSnapshotDigestsEnvironmentRevisionAndReferenceProvenance` | `Run Snapshot Digests Environment Revision And Reference Provenance`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_snapshot_corrections_test.go`](../../domain/execution/run_snapshot_corrections_test.go) · `TestRunSnapshotDigestsEnvironmentRevisionAndReferenceProvenance` |
| `TestSealRunSnapshotRejectsZeroEnvironmentRevision` | `Seal Run Snapshot Rejects Zero Environment Revision`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_snapshot_corrections_test.go`](../../domain/execution/run_snapshot_corrections_test.go) · `TestSealRunSnapshotRejectsZeroEnvironmentRevision` |
| `TestHydrateRunSnapshotRejectsZeroEnvironmentRevisionBeforeStoredDigest` | `Hydrate Run Snapshot Rejects Zero Environment Revision Before Stored Digest`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_snapshot_corrections_test.go`](../../domain/execution/run_snapshot_corrections_test.go) · `TestHydrateRunSnapshotRejectsZeroEnvironmentRevisionBeforeStoredDigest` |
| `TestHydrateRunSnapshotVerifiesStoredDigest` | `Hydrate Run Snapshot Verifies Stored Digest`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_snapshot_corrections_test.go`](../../domain/execution/run_snapshot_corrections_test.go) · `TestHydrateRunSnapshotVerifiesStoredDigest` |
| `TestPolicyNegativeZeroHasCanonicalDigest` | `Policy Negative Zero Has Canonical Digest`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_snapshot_corrections_test.go`](../../domain/execution/run_snapshot_corrections_test.go) · `TestPolicyNegativeZeroHasCanonicalDigest` |
| `TestNewRunRejectsInvalidInitialLifecycleShape` | `New Run Rejects Invalid Initial Lifecycle Shape`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_snapshot_corrections_test.go`](../../domain/execution/run_snapshot_corrections_test.go) · `TestNewRunRejectsInvalidInitialLifecycleShape` |
| `TestSealRunSnapshotRejectsConcreteChildValuesOutsideTargetParameterContract` | `Seal Run Snapshot Rejects Concrete Child Values Outside Target Parameter Contract`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_snapshot_invariants_test.go`](../../domain/execution/run_snapshot_invariants_test.go) · `TestSealRunSnapshotRejectsConcreteChildValuesOutsideTargetParameterContract` |
| `TestRunSnapshotRejectsTestTaskEntryBijectionDefects` | `Run Snapshot Rejects Test Task Entry Bijection Defects`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_snapshot_invariants_test.go`](../../domain/execution/run_snapshot_invariants_test.go) · `TestRunSnapshotRejectsTestTaskEntryBijectionDefects` |
| `TestValidateBindingsRejectsUnknownLiteralAndParentNames` | `Validate Bindings Rejects Unknown Literal And Parent Names`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_snapshot_invariants_test.go`](../../domain/execution/run_snapshot_invariants_test.go) · `TestValidateBindingsRejectsUnknownLiteralAndParentNames` |
| `TestRunSnapshotAggregateStringBytesIncludesEnvironmentAndInvocation` | `Run Snapshot Aggregate String Bytes Includes Environment And Invocation`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_snapshot_invariants_test.go`](../../domain/execution/run_snapshot_invariants_test.go) · `TestRunSnapshotAggregateStringBytesIncludesEnvironmentAndInvocation` |
| `TestRunSnapshotSharesAggregateStringBudgetAcrossPlanAndEnvelope` | `Run Snapshot Shares Aggregate String Budget Across Plan And Envelope`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_snapshot_invariants_test.go`](../../domain/execution/run_snapshot_invariants_test.go) · `TestRunSnapshotSharesAggregateStringBudgetAcrossPlanAndEnvelope` |
| `TestConsumeElementsRejectsOverflowShapedCountWithoutAllocation` | `Consume Elements Rejects Overflow Shaped Count Without Allocation`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_snapshot_invariants_test.go`](../../domain/execution/run_snapshot_invariants_test.go) · `TestConsumeElementsRejectsOverflowShapedCountWithoutAllocation` |
| `TestBuildSnapshotValidationIndexesIncludesEveryWorkflowReference` | `Build Snapshot Validation Indexes Includes Every Workflow Reference`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_snapshot_performance_test.go`](../../domain/execution/run_snapshot_performance_test.go) · `TestBuildSnapshotValidationIndexesIncludesEveryWorkflowReference` |
| `TestBuildSnapshotValidationIndexesIncludesEveryEntry` | `Build Snapshot Validation Indexes Includes Every Entry`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_snapshot_performance_test.go`](../../domain/execution/run_snapshot_performance_test.go) · `TestBuildSnapshotValidationIndexesIncludesEveryEntry` |
| `TestValidateSnapshotIndexesTestTaskVersionItems` | `Validate Snapshot Indexes Test Task Version Items`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_snapshot_performance_test.go`](../../domain/execution/run_snapshot_performance_test.go) · `TestValidateSnapshotIndexesTestTaskVersionItems` |
| `TestRunSnapshotInvocationOrderIsCanonicalAndDigestIndependent` | `Run Snapshot Invocation Order Is Canonical And Digest Independent`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_snapshot_test.go`](../../domain/execution/run_snapshot_test.go) · `TestRunSnapshotInvocationOrderIsCanonicalAndDigestIndependent` |
| `TestRunSnapshotRejectsInvocationWithMissingParentIndependentOfOrder` | `Run Snapshot Rejects Invocation With Missing Parent Independent Of Order`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_snapshot_test.go`](../../domain/execution/run_snapshot_test.go) · `TestRunSnapshotRejectsInvocationWithMissingParentIndependentOfOrder` |
| `TestRunSnapshotRejectsInvocationParentCycle` | `Run Snapshot Rejects Invocation Parent Cycle`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_snapshot_test.go`](../../domain/execution/run_snapshot_test.go) · `TestRunSnapshotRejectsInvocationParentCycle` |
| `TestSealRunSnapshotOwnsDataAndHasStableCanonicalDigest` | `Seal Run Snapshot Owns Data And Has Stable Canonical Digest`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_snapshot_test.go`](../../domain/execution/run_snapshot_test.go) · `TestSealRunSnapshotOwnsDataAndHasStableCanonicalDigest` |
| `TestRunSnapshotDigestChangesForExecutionRelevantCategories` | `Run Snapshot Digest Changes For Execution Relevant Categories`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_snapshot_test.go`](../../domain/execution/run_snapshot_test.go) · `TestRunSnapshotDigestChangesForExecutionRelevantCategories` |
| `TestSealRunSnapshotRejectsInvalidIdentityEnvironmentAndPolicies` | `Seal Run Snapshot Rejects Invalid Identity Environment And Policies`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_snapshot_test.go`](../../domain/execution/run_snapshot_test.go) · `TestSealRunSnapshotRejectsInvalidIdentityEnvironmentAndPolicies` |
| `TestRunSnapshotInputExportSupportsDurableHydrationWithoutAliasing` | `Run Snapshot Input Export Supports Durable Hydration Without Aliasing`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_snapshot_test.go`](../../domain/execution/run_snapshot_test.go) · `TestRunSnapshotInputExportSupportsDurableHydrationWithoutAliasing` |
| `TestHydrateRunRestoresPrivateSnapshotSeal` | `Hydrate Run Restores Private Snapshot Seal`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_snapshot_test.go`](../../domain/execution/run_snapshot_test.go) · `TestHydrateRunRestoresPrivateSnapshotSeal` |
| `TestRunTransitionPreservesSnapshotIdentity` | `Run Transition Preserves Snapshot Identity`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_snapshot_test.go`](../../domain/execution/run_snapshot_test.go) · `TestRunTransitionPreservesSnapshotIdentity` |
| `TestRunTransitionRejectsMalformedReceiverLifecycle` | `Run Transition Rejects Malformed Receiver Lifecycle`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_test.go`](../../domain/execution/run_test.go) · `TestRunTransitionRejectsMalformedReceiverLifecycle` |
| `TestRunTransitionRoundTripsThroughHydration` | `Run Transition Round Trips Through Hydration`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_test.go`](../../domain/execution/run_test.go) · `TestRunTransitionRoundTripsThroughHydration` |
| `TestHydrateRunEnforcesPersistedLifecycleShapes` | `Hydrate Run Enforces Persisted Lifecycle Shapes`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_test.go`](../../domain/execution/run_test.go) · `TestHydrateRunEnforcesPersistedLifecycleShapes` |
| `TestRunStatusTransitionPermitsCancellationBeforeAndDuringExecution` | `Run Status Transition Permits Cancellation Before And During Execution`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_test.go`](../../domain/execution/run_test.go) · `TestRunStatusTransitionPermitsCancellationBeforeAndDuringExecution` |
| `TestRunTransitionAcceptsLifecycleProgression` | `Run Transition Accepts Lifecycle Progression`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_test.go`](../../domain/execution/run_test.go) · `TestRunTransitionAcceptsLifecycleProgression` |
| `TestRunTransitionRejectsReopeningTerminalRun` | `Run Transition Rejects Reopening Terminal Run`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_test.go`](../../domain/execution/run_test.go) · `TestRunTransitionRejectsReopeningTerminalRun` |
| `TestRunTransitionRejectsAbortingQueuedRun` | `Run Transition Rejects Aborting Queued Run`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/run_test.go`](../../domain/execution/run_test.go) · `TestRunTransitionRejectsAbortingQueuedRun` |
| `TestWorkflowSnapshotValidateRejectsMalformedExecutionContracts` | `Workflow Snapshot Validate Rejects Malformed Execution Contracts`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestWorkflowSnapshotValidateRejectsMalformedExecutionContracts` |
| `TestDraftValidateRejectsInvalidNodeSnapshots` | `Draft Validate Rejects Invalid Node Snapshots`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestDraftValidateRejectsInvalidNodeSnapshots` |
| `TestDraftValidateRejectsNodeVersionOwnedByDifferentNodes` | `Draft Validate Rejects Node Version Owned By Different Nodes`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestDraftValidateRejectsNodeVersionOwnedByDifferentNodes` |
| `TestDraftValidateRejectsReachableWorkflowReferenceCycles` | `Draft Validate Rejects Reachable Workflow Reference Cycles`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestDraftValidateRejectsReachableWorkflowReferenceCycles` |
| `TestPlanSealState` | `Plan Seal State`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestPlanSealState` |
| `TestSealRejectsBlankAndMismatchedFixedWorkflowVersions` | `Seal Rejects Blank And Mismatched Fixed Workflow Versions`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestSealRejectsBlankAndMismatchedFixedWorkflowVersions` |
| `TestSealOwnsNestedMutableValues` | `Seal Owns Nested Mutable Values`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestSealOwnsNestedMutableValues` |
| `TestWorkflowSnapshotValidateBoundsNestingWithoutOverflow` | `Workflow Snapshot Validate Bounds Nesting Without Overflow`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestWorkflowSnapshotValidateBoundsNestingWithoutOverflow` |
| `TestParameterValidateRequiresCompleteDefinition` | `Parameter Validate Requires Complete Definition`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestParameterValidateRequiresCompleteDefinition` |
| `TestSealedPlanAccessorsAndNestedCopies` | `Sealed Plan Accessors And Nested Copies`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestSealedPlanAccessorsAndNestedCopies` |
| `TestValidationValidateBranches` | `Validation Validate Branches`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestValidationValidateBranches` |
| `TestDraftValidateAggregatesReferencedWorkflowBudgetAcrossCallsAndRepeats` | `Draft Validate Aggregates Referenced Workflow Budget Across Calls And Repeats`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestDraftValidateAggregatesReferencedWorkflowBudgetAcrossCallsAndRepeats` |
| `TestExecutionBudgetCountsValidationBranchWork` | `Execution Budget Counts Validation Branch Work`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestExecutionBudgetCountsValidationBranchWork` |
| `TestExecutionBudgetMemoizesSharedDiamondAndChargesEveryCallSite` | `Execution Budget Memoizes Shared Diamond And Charges Every Call Site`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestExecutionBudgetMemoizesSharedDiamondAndChargesEveryCallSite` |
| `TestExecutionBudgetSharedDiamondGrowthIsLinear` | `Execution Budget Shared Diamond Growth Is Linear`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestExecutionBudgetSharedDiamondGrowthIsLinear` |
| `TestDraftValidateReportsOrphanResolutionDeterministically` | `Draft Validate Reports Orphan Resolution Deterministically`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestDraftValidateReportsOrphanResolutionDeterministically` |
| `TestNavigateSealValidation` | `Navigate Seal Validation`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestNavigateSealValidation` |
| `TestAggregateInputLimitsRejectTopLevelEntriesBeforeSealClone` | `Aggregate Input Limits Reject Top Level Entries Before Seal Clone`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestAggregateInputLimitsRejectTopLevelEntriesBeforeSealClone` |
| `TestAggregateInputLimitsBeforeSealClone` | `Aggregate Input Limits Before Seal Clone`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestAggregateInputLimitsBeforeSealClone` |
| `TestExecutionBudgetAggregatesAllEntryOccurrences` | `Execution Budget Aggregates All Entry Occurrences`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestExecutionBudgetAggregatesAllEntryOccurrences` |
| `TestExecutionBudgetChargesSharedTargetPerEntryButEvaluatesOnce` | `Execution Budget Charges Shared Target Per Entry But Evaluates Once`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestExecutionBudgetChargesSharedTargetPerEntryButEvaluatesOnce` |
| `TestExecutionBudgetAcceptsExactAggregateLimits` | `Execution Budget Accepts Exact Aggregate Limits`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestExecutionBudgetAcceptsExactAggregateLimits` |
| `TestAggregateCollectionLimitsRejectEmptyEntriesBeforeSealClone` | `Aggregate Collection Limits Reject Empty Entries Before Seal Clone`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestAggregateCollectionLimitsRejectEmptyEntriesBeforeSealClone` |
| `TestExecutionStatusTransitionMatrix` | `Execution Status Transition Matrix`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestExecutionStatusTransitionMatrix` |
| `TestExecutionStatusRejectsUnknownAndTerminalTransitions` | `Execution Status Rejects Unknown And Terminal Transitions`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestExecutionStatusRejectsUnknownAndTerminalTransitions` |

## Cross-cutting / Conformance Cases

同包及其子目录中名称含 `Conformance`、`Transaction`、`Race`、`Rollback`、`Replay`、`Concurrent` 或 `Fence` 的测试，属于跨入口契约；它们已在上方矩阵逐行列出。application 包的 `conformancetest/` 证据也归属此表。

## 维护规则

1. 新增或删除 `Test…` 函数时，必须同步更新本表；表驱动新增子案例要更新相应行的边界描述。
2. 新增公开 domain API 或 application use case 时，必须先添加公开入口清单行和至少一条可执行测试证据。
3. 文档不替代测试；冲突时以 Go 测试断言和领域契约为准。
