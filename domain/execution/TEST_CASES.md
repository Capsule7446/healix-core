# domain/execution Test Case Matrix

## 范围与口径

本表记录 `domain/execution` 的公开业务入口和全部顶层 Go testcase。Go 测试源码是唯一可执行事实；表驱动测试的全部子案例由其对应的测试函数统一引用。

## Public API / Use-case Inventory

| 公开入口 | 定义文件 | 测试证据状态 |
|---|---|---|
| `FailurePolicy.IsValid` | [`domain/execution/plan.go`](../../domain/execution/plan.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `Seal` | [`domain/execution/plan.go`](../../domain/execution/plan.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `Plan.IsSealed` | [`domain/execution/plan.go`](../../domain/execution/plan.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `Plan.Validate` | [`domain/execution/plan.go`](../../domain/execution/plan.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `Plan.Snapshot` | [`domain/execution/plan.go`](../../domain/execution/plan.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `Plan.RunID` | [`domain/execution/plan.go`](../../domain/execution/plan.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `Plan.FailurePolicy` | [`domain/execution/plan.go`](../../domain/execution/plan.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `Plan.Entries` | [`domain/execution/plan.go`](../../domain/execution/plan.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `Plan.Workflows` | [`domain/execution/plan.go`](../../domain/execution/plan.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `Plan.Nodes` | [`domain/execution/plan.go`](../../domain/execution/plan.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `Plan.References` | [`domain/execution/plan.go`](../../domain/execution/plan.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `ValidateRunStatusTransition` | [`domain/execution/instance.go`](../../domain/execution/instance.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `NewRun` | [`domain/execution/instance.go`](../../domain/execution/instance.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `HydrateRun` | [`domain/execution/instance.go`](../../domain/execution/instance.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `ValidateRun` | [`domain/execution/instance.go`](../../domain/execution/instance.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `Run.Transition` | [`domain/execution/instance.go`](../../domain/execution/instance.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `DefaultHealerPolicySnapshot` | [`domain/execution/instance_snapshot.go`](../../domain/execution/instance_snapshot.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `RunSnapshot.Digest` | [`domain/execution/instance_snapshot.go`](../../domain/execution/instance_snapshot.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `RunSnapshot.SchemaVersion` | [`domain/execution/instance_snapshot.go`](../../domain/execution/instance_snapshot.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `RunSnapshot.RunID` | [`domain/execution/instance_snapshot.go`](../../domain/execution/instance_snapshot.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `RunSnapshot.TestTaskID` | [`domain/execution/instance_snapshot.go`](../../domain/execution/instance_snapshot.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `RunSnapshot.TestTaskVersionID` | [`domain/execution/instance_snapshot.go`](../../domain/execution/instance_snapshot.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `RunSnapshot.Input` | [`domain/execution/instance_snapshot.go`](../../domain/execution/instance_snapshot.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `RunSnapshot.Plan` | [`domain/execution/instance_snapshot.go`](../../domain/execution/instance_snapshot.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `RunSnapshot.Invocations` | [`domain/execution/instance_snapshot.go`](../../domain/execution/instance_snapshot.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `RunSnapshot.Invocation` | [`domain/execution/instance_snapshot.go`](../../domain/execution/instance_snapshot.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `RunSnapshot.Environment` | [`domain/execution/instance_snapshot.go`](../../domain/execution/instance_snapshot.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `SealRunSnapshot` | [`domain/execution/instance_snapshot.go`](../../domain/execution/instance_snapshot.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `HydrateRunSnapshot` | [`domain/execution/instance_snapshot.go`](../../domain/execution/instance_snapshot.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `ValidateExecutionStatusTransition` | [`domain/execution/status.go`](../../domain/execution/status.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `IsTerminalExecutionStatus` | [`domain/execution/status.go`](../../domain/execution/status.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `ExecutionStatus.CanTransitionTo` | [`domain/execution/status.go`](../../domain/execution/status.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `Parameter.Validate` | [`domain/execution/validation.go`](../../domain/execution/validation.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `Draft.Validate` | [`domain/execution/validation.go`](../../domain/execution/validation.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `WorkflowSnapshot.Validate` | [`domain/execution/step_shape.go`](../../domain/execution/step_shape.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `Validation.Validate` | [`domain/execution/step_shape.go`](../../domain/execution/step_shape.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `WorkerFence.Validate` | [`domain/execution/worker_fence.go`](../../domain/execution/worker_fence.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `StaleWorkerFenceError.Error` | [`domain/execution/worker_fence.go`](../../domain/execution/worker_fence.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |
| `StaleWorkerFenceError.Is` | [`domain/execution/worker_fence.go`](../../domain/execution/worker_fence.go) | 在下方 testcase 矩阵中提供直接行为证据；无业务分支的辅助 accessor 以调用方契约覆盖。 |

## Test Case Evidence Matrix

| Test case | 输入、边界或业务前置状态 | 预期契约 | 可执行证据 |
|---|---|---|---|
| `TestFailurePolicyAndPlanFailurePolicyDirect` | `Failure Policy And Plan Failure Policy Direct`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/direct_gap_test.go`](../../domain/execution/direct_gap_test.go) · `TestFailurePolicyAndPlanFailurePolicyDirect` |
| `TestValidateInstanceDirect` | `Validate Run Direct`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/direct_gap_test.go`](../../domain/execution/direct_gap_test.go) · `TestValidateInstanceDirect` |
| `TestInstanceSnapshotNamedAccessorsAndInvocationIsolationDirect` | `Run Snapshot Named Accessors And Invocation Isolation Direct`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/direct_gap_test.go`](../../domain/execution/direct_gap_test.go) · `TestInstanceSnapshotNamedAccessorsAndInvocationIsolationDirect` |
| `TestEntryStatusDirect` | `Execution Status Direct`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/direct_gap_test.go`](../../domain/execution/direct_gap_test.go) · `TestEntryStatusDirect` |
| `TestInvocationEdgeKeyDoesNotCollideLikeDelimitedStrings` | `Invocation Edge Key Does Not Collide Like Delimited Strings`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/invocation_edge_test.go`](../../domain/execution/invocation_edge_test.go) · `TestInvocationEdgeKeyDoesNotCollideLikeDelimitedStrings` |
| `TestInstanceSnapshotRequiresCanonicalChildInvocationPath` | `Run Snapshot Requires Canonical Child Invocation Path`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/invocation_edge_test.go`](../../domain/execution/invocation_edge_test.go) · `TestInstanceSnapshotRequiresCanonicalChildInvocationPath` |
| `TestInstanceSnapshotUsesConcreteParentPathForRepeatedReferenceEdges` | `Run Snapshot Uses Concrete Parent Path For Repeated Reference Edges`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/invocation_edge_test.go`](../../domain/execution/invocation_edge_test.go) · `TestInstanceSnapshotUsesConcreteParentPathForRepeatedReferenceEdges` |
| `TestInstanceSnapshotRequiresCompleteConcreteBindingsAndChildValues` | `Run Snapshot Requires Complete Concrete Bindings And Child Values`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/invocation_edge_test.go`](../../domain/execution/invocation_edge_test.go) · `TestInstanceSnapshotRequiresCompleteConcreteBindingsAndChildValues` |
| `TestInstanceSnapshotPermitsConcreteBindingDifferentFromAuthoringMetadata` | `Run Snapshot Permits Concrete Binding Different From Authoring Metadata`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/invocation_edge_test.go`](../../domain/execution/invocation_edge_test.go) · `TestInstanceSnapshotPermitsConcreteBindingDifferentFromAuthoringMetadata` |
| `TestSealedNavigationURLSecurityMatrix` | 46 个用例，每条生产规则一行、每个可能的绕过一行：非 http(s) scheme（`javascript:`/`data:`/`file:`/`ftp:`/`about:`/`chrome:`/`view-source:`/协议相对/相对路径/裸主机/空串）、userinfo（含 `user:pass@`、空 userinfo、`trusted.test@evil.test` 伪装）、空 host、控制字符（NUL/CR/LF/TAB/DEL/ESC）、scheme 与 authority 内插值（含 host/scheme/port/userinfo/整串/authority 结束前）、以及必须继续放行的正常 URL（显式端口、IPv4、IPv6、国际化域名、百分号编码、path/query/fragment 插值）。 | 每个用例断言 `validateSealedNavigationURL` 的接受或拒绝与该行声明一致；放行组存在的意义是让「全部拒绝」的实现无法通过。 | [`domain/execution/navigation_security_test.go`](../../domain/execution/navigation_security_test.go) · `TestSealedNavigationURLSecurityMatrix` |
| `TestSealedNavigationURLDefersLengthToTheAggregateBound` | URL 规则本身不设长度上限，长度由 plan 级 `MaxStringBytes` 统一约束。 | 断言超长 URL 通过 `validateSealedNavigationURL` 但被 `validateAggregateInputBounds` 拒绝——把这个分工记为决定，而非日后偶然出现的第二个限值。 | [`domain/execution/navigation_security_test.go`](../../domain/execution/navigation_security_test.go) · `TestSealedNavigationURLDefersLengthToTheAggregateBound` |
| `TestSealedNavigationURLRejectionsStayValueFree` | 被拒 URL 是调用方输入，query 里可能带 token。 | 断言拒绝文案既不回显整条 URL，也不含 `hunter2`/`s3cr3t`/`document.cookie` 等片段。 | [`domain/execution/navigation_security_test.go`](../../domain/execution/navigation_security_test.go) · `TestSealedNavigationURLRejectionsStayValueFree` |
| `TestSealIndependentlyRejectsInvalidParameterDefinitionsAndValues` | `Seal Independently Rejects Invalid Parameter Definitions And Values`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/parameter_validation_test.go`](../../domain/execution/parameter_validation_test.go) · `TestSealIndependentlyRejectsInvalidParameterDefinitionsAndValues` |
| `TestSealRejectsInvalidSnapshotForParameterlessWorkflow` | `Seal Rejects Invalid Snapshot For Parameterless Workflow`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/parameter_validation_test.go`](../../domain/execution/parameter_validation_test.go) · `TestSealRejectsInvalidSnapshotForParameterlessWorkflow` |
| `TestSealRejectsMismatchedParameterizedSnapshotIdentity` | `Seal Rejects Mismatched Parameterized Snapshot Identity`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/parameter_validation_test.go`](../../domain/execution/parameter_validation_test.go) · `TestSealRejectsMismatchedParameterizedSnapshotIdentity` |
| `TestSealAccountsForParameterOptionCollectionElements` | `Seal Accounts For Parameter Option Collection Elements`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/parameter_validation_test.go`](../../domain/execution/parameter_validation_test.go) · `TestSealAccountsForParameterOptionCollectionElements` |
| `TestSealAccountsForTypedSnapshotAndMultiSelectResources` | `Seal Accounts For Typed Snapshot And Multi Select Resources`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/parameter_validation_test.go`](../../domain/execution/parameter_validation_test.go) · `TestSealAccountsForTypedSnapshotAndMultiSelectResources` |
| `TestSealInstanceSnapshotRejectsBindingsOnRootInvocation` | `Seal Run Snapshot Rejects Bindings On Root Invocation`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_snapshot_corrections_test.go`](../../domain/execution/instance_snapshot_corrections_test.go) · `TestSealInstanceSnapshotRejectsBindingsOnRootInvocation` |
| `TestInstanceSnapshotFreezesCompleteExecutionPlanAndInvocationScopes` | `Run Snapshot Freezes Complete Execution Plan And Invocation Scopes`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_snapshot_corrections_test.go`](../../domain/execution/instance_snapshot_corrections_test.go) · `TestInstanceSnapshotFreezesCompleteExecutionPlanAndInvocationScopes` |
| `TestInstanceSnapshotDigestsEnvironmentRevisionAndReferenceProvenance` | `Run Snapshot Digests Environment Revision And Reference Provenance`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_snapshot_corrections_test.go`](../../domain/execution/instance_snapshot_corrections_test.go) · `TestInstanceSnapshotDigestsEnvironmentRevisionAndReferenceProvenance` |
| `TestSealInstanceSnapshotRejectsZeroEnvironmentRevision` | `Seal Run Snapshot Rejects Zero Environment Revision`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_snapshot_corrections_test.go`](../../domain/execution/instance_snapshot_corrections_test.go) · `TestSealInstanceSnapshotRejectsZeroEnvironmentRevision` |
| `TestHydrateInstanceSnapshotRejectsZeroEnvironmentRevisionBeforeStoredDigest` | `Hydrate Run Snapshot Rejects Zero Environment Revision Before Stored Digest`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_snapshot_corrections_test.go`](../../domain/execution/instance_snapshot_corrections_test.go) · `TestHydrateInstanceSnapshotRejectsZeroEnvironmentRevisionBeforeStoredDigest` |
| `TestHydrateInstanceSnapshotVerifiesStoredDigest` | `Hydrate Run Snapshot Verifies Stored Digest`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_snapshot_corrections_test.go`](../../domain/execution/instance_snapshot_corrections_test.go) · `TestHydrateInstanceSnapshotVerifiesStoredDigest` |
| `TestPolicyNegativeZeroHasCanonicalDigest` | `Policy Negative Zero Has Canonical Digest`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_snapshot_corrections_test.go`](../../domain/execution/instance_snapshot_corrections_test.go) · `TestPolicyNegativeZeroHasCanonicalDigest` |
| `TestNewInstanceRejectsInvalidInitialLifecycleShape` | `New Run Rejects Invalid Initial Lifecycle Shape`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_snapshot_corrections_test.go`](../../domain/execution/instance_snapshot_corrections_test.go) · `TestNewInstanceRejectsInvalidInitialLifecycleShape` |
| `TestSealInstanceSnapshotRejectsConcreteChildValuesOutsideTargetParameterContract` | `Seal Run Snapshot Rejects Concrete Child Values Outside Target Parameter Contract`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_snapshot_invariants_test.go`](../../domain/execution/instance_snapshot_invariants_test.go) · `TestSealInstanceSnapshotRejectsConcreteChildValuesOutsideTargetParameterContract` |
| `TestInstanceSnapshotRejectsTestTaskEntryBijectionDefects` | `Run Snapshot Rejects Test Task Entry Bijection Defects`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_snapshot_invariants_test.go`](../../domain/execution/instance_snapshot_invariants_test.go) · `TestInstanceSnapshotRejectsTestTaskEntryBijectionDefects` |
| `TestValidateBindingsRejectsUnknownLiteralAndParentNames` | `Validate Bindings Rejects Unknown Literal And Parent Names`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_snapshot_invariants_test.go`](../../domain/execution/instance_snapshot_invariants_test.go) · `TestValidateBindingsRejectsUnknownLiteralAndParentNames` |
| `TestInstanceSnapshotAggregateStringBytesIncludesEnvironmentAndInvocation` | `Run Snapshot Aggregate String Bytes Includes Environment And Invocation`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_snapshot_invariants_test.go`](../../domain/execution/instance_snapshot_invariants_test.go) · `TestInstanceSnapshotAggregateStringBytesIncludesEnvironmentAndInvocation` |
| `TestInstanceSnapshotSharesAggregateStringBudgetAcrossPlanAndEnvelope` | `Run Snapshot Shares Aggregate String Budget Across Plan And Envelope`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_snapshot_invariants_test.go`](../../domain/execution/instance_snapshot_invariants_test.go) · `TestInstanceSnapshotSharesAggregateStringBudgetAcrossPlanAndEnvelope` |
| `TestConsumeElementsRejectsOverflowShapedCountWithoutAllocation` | `Consume Elements Rejects Overflow Shaped Count Without Allocation`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_snapshot_invariants_test.go`](../../domain/execution/instance_snapshot_invariants_test.go) · `TestConsumeElementsRejectsOverflowShapedCountWithoutAllocation` |
| `TestBuildSnapshotValidationIndexesIncludesEveryWorkflowReference` | `Build Snapshot Validation Indexes Includes Every Workflow Reference`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_snapshot_performance_test.go`](../../domain/execution/instance_snapshot_performance_test.go) · `TestBuildSnapshotValidationIndexesIncludesEveryWorkflowReference` |
| `TestBuildSnapshotValidationIndexesIncludesEveryEntry` | `Build Snapshot Validation Indexes Includes Every Entry`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_snapshot_performance_test.go`](../../domain/execution/instance_snapshot_performance_test.go) · `TestBuildSnapshotValidationIndexesIncludesEveryEntry` |
| `TestValidateSnapshotIndexesTestTaskVersionItems` | `Validate Snapshot Indexes Test Task Version Items`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_snapshot_performance_test.go`](../../domain/execution/instance_snapshot_performance_test.go) · `TestValidateSnapshotIndexesTestTaskVersionItems` |
| `TestInstanceSnapshotInvocationOrderIsCanonicalAndDigestIndependent` | `Run Snapshot Invocation Order Is Canonical And Digest Independent`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_snapshot_test.go`](../../domain/execution/instance_snapshot_test.go) · `TestInstanceSnapshotInvocationOrderIsCanonicalAndDigestIndependent` |
| `TestInstanceSnapshotRejectsInvocationWithMissingParentIndependentOfOrder` | `Run Snapshot Rejects Invocation With Missing Parent Independent Of Order`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_snapshot_test.go`](../../domain/execution/instance_snapshot_test.go) · `TestInstanceSnapshotRejectsInvocationWithMissingParentIndependentOfOrder` |
| `TestInstanceSnapshotRejectsInvocationParentCycle` | `Run Snapshot Rejects Invocation Parent Cycle`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_snapshot_test.go`](../../domain/execution/instance_snapshot_test.go) · `TestInstanceSnapshotRejectsInvocationParentCycle` |
| `TestSealInstanceSnapshotOwnsDataAndHasStableCanonicalDigest` | `Seal Run Snapshot Owns Data And Has Stable Canonical Digest`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_snapshot_test.go`](../../domain/execution/instance_snapshot_test.go) · `TestSealInstanceSnapshotOwnsDataAndHasStableCanonicalDigest` |
| `TestInstanceSnapshotDigestChangesForExecutionRelevantCategories` | `Run Snapshot Digest Changes For Execution Relevant Categories`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_snapshot_test.go`](../../domain/execution/instance_snapshot_test.go) · `TestInstanceSnapshotDigestChangesForExecutionRelevantCategories` |
| `TestSealInstanceSnapshotRejectsInvalidIdentityEnvironmentAndPolicies` | `Seal Run Snapshot Rejects Invalid Identity Environment And Policies`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_snapshot_test.go`](../../domain/execution/instance_snapshot_test.go) · `TestSealInstanceSnapshotRejectsInvalidIdentityEnvironmentAndPolicies` |
| `TestInstanceSnapshotInputExportSupportsDurableHydrationWithoutAliasing` | `Run Snapshot Input Export Supports Durable Hydration Without Aliasing`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_snapshot_test.go`](../../domain/execution/instance_snapshot_test.go) · `TestInstanceSnapshotInputExportSupportsDurableHydrationWithoutAliasing` |
| `TestHydrateInstanceRestoresPrivateSnapshotSeal` | `Hydrate Run Restores Private Snapshot Seal`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_snapshot_test.go`](../../domain/execution/instance_snapshot_test.go) · `TestHydrateInstanceRestoresPrivateSnapshotSeal` |
| `TestInstanceTransitionPreservesSnapshotIdentity` | `Run Transition Preserves Snapshot Identity`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_snapshot_test.go`](../../domain/execution/instance_snapshot_test.go) · `TestInstanceTransitionPreservesSnapshotIdentity` |
| `TestInstanceSnapshotEnvironmentNamesUseSharedValidation` | `Instance Snapshot Environment Names Use Shared Validation`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_snapshot_test.go`](../../domain/execution/instance_snapshot_test.go) · `TestInstanceSnapshotEnvironmentNamesUseSharedValidation` |
| `TestInstanceSnapshotV1DigestAndTypedHydrationRemainCompatible` | `Instance Snapshot V1 Digest And Typed Hydration Remain Compatible`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_snapshot_test.go`](../../domain/execution/instance_snapshot_test.go) · `TestInstanceSnapshotV1DigestAndTypedHydrationRemainCompatible` |
| `TestInstanceSnapshotV2DigestIsStableTypeSensitiveAndOwnsMultiSelect` | `Instance Snapshot V2 Digest Is Stable Type Sensitive And Owns Multi Select`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_snapshot_test.go`](../../domain/execution/instance_snapshot_test.go) · `TestInstanceSnapshotV2DigestIsStableTypeSensitiveAndOwnsMultiSelect` |
| `TestInstanceTransitionRejectsMalformedReceiverLifecycle` | `Run Transition Rejects Malformed Receiver Lifecycle`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_test.go`](../../domain/execution/instance_test.go) · `TestInstanceTransitionRejectsMalformedReceiverLifecycle` |
| `TestInstanceTransitionRoundTripsThroughHydration` | `Run Transition Round Trips Through Hydration`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_test.go`](../../domain/execution/instance_test.go) · `TestInstanceTransitionRoundTripsThroughHydration` |
| `TestHydrateInstanceEnforcesPersistedLifecycleShapes` | `Hydrate Run Enforces Persisted Lifecycle Shapes`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_test.go`](../../domain/execution/instance_test.go) · `TestHydrateInstanceEnforcesPersistedLifecycleShapes` |
| `TestInstanceStatusTransitionPermitsCancellationBeforeAndDuringExecution` | `Run Status Transition Permits Cancellation Before And During Execution`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_test.go`](../../domain/execution/instance_test.go) · `TestInstanceStatusTransitionPermitsCancellationBeforeAndDuringExecution` |
| `TestInstanceTransitionAcceptsLifecycleProgression` | `Run Transition Accepts Lifecycle Progression`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_test.go`](../../domain/execution/instance_test.go) · `TestInstanceTransitionAcceptsLifecycleProgression` |
| `TestInstanceTransitionRejectsReopeningTerminalRun` | `Run Transition Rejects Reopening Terminal Run`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_test.go`](../../domain/execution/instance_test.go) · `TestInstanceTransitionRejectsReopeningTerminalRun` |
| `TestInstanceTransitionRejectsAbortingQueuedRun` | `Run Transition Rejects Aborting Queued Run`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/instance_test.go`](../../domain/execution/instance_test.go) · `TestInstanceTransitionRejectsAbortingQueuedRun` |
| `TestWorkflowSnapshotValidateRejectsMalformedExecutionContracts` | `Workflow Snapshot Validate Rejects Malformed Execution Contracts`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestWorkflowSnapshotValidateRejectsMalformedExecutionContracts` |
| `TestDraftValidateRejectsInvalidNodeSnapshots` | `Draft Validate Rejects Invalid Node Snapshots`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestDraftValidateRejectsInvalidNodeSnapshots` |
| `TestDraftValidateRejectsNodeVersionOwnedByDifferentNodes` | `Draft Validate Rejects Node Version Owned By Different Nodes`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestDraftValidateRejectsNodeVersionOwnedByDifferentNodes` |
| `TestDraftValidateRejectsReachableWorkflowReferenceCycles` | `Draft Validate Rejects Reachable Workflow Reference Cycles`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestDraftValidateRejectsReachableWorkflowReferenceCycles` |
| `TestPlanSealState` | `Plan Seal State`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestPlanSealState` |
| `TestSealRejectsBlankAndMismatchedFixedWorkflowVersions` | `Seal Rejects Blank And Mismatched Fixed Workflow Versions`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestSealRejectsBlankAndMismatchedFixedWorkflowVersions` |
| `TestSealOwnsNestedMutableValues` | `Seal Owns Nested Mutable Values`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestSealOwnsNestedMutableValues` |
| `TestWorkflowSnapshotValidateBoundsNestingWithoutOverflow` | `Workflow Snapshot Validate Bounds Nesting Without Overflow`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestWorkflowSnapshotValidateBoundsNestingWithoutOverflow` |
| `TestParameterValidateRequiresCompleteDefinition` | `Parameter Validate Requires Complete Definition`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestParameterValidateRequiresCompleteDefinition` |
| `TestSealedPlanAccessorsAndNestedCopies` | `Sealed Plan Accessors And Nested Copies`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestSealedPlanAccessorsAndNestedCopies` |
| `TestValidationValidateBranches` | `Validation Validate Branches`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestValidationValidateBranches` |
| `TestDraftValidateAggregatesReferencedWorkflowBudgetAcrossCallsAndRepeats` | `Draft Validate Aggregates Referenced Workflow Budget Across Calls And Repeats`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestDraftValidateAggregatesReferencedWorkflowBudgetAcrossCallsAndRepeats` |
| `TestExecutionBudgetCountsValidationBranchWork` | `Execution Budget Counts Validation Branch Work`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestExecutionBudgetCountsValidationBranchWork` |
| `TestExecutionBudgetMemoizesSharedDiamondAndChargesEveryCallSite` | `Execution Budget Memoizes Shared Diamond And Charges Every Call Site`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestExecutionBudgetMemoizesSharedDiamondAndChargesEveryCallSite` |
| `TestExecutionBudgetSharedDiamondGrowthIsLinear` | `Execution Budget Shared Diamond Growth Is Linear`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestExecutionBudgetSharedDiamondGrowthIsLinear` |
| `TestDraftValidateReportsOrphanResolutionDeterministically` | `Draft Validate Reports Orphan Resolution Deterministically`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestDraftValidateReportsOrphanResolutionDeterministically` |
| `TestNavigateSealValidation` | `Navigate Seal Validation`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestNavigateSealValidation` |
| `TestAggregateInputLimitsRejectTopLevelEntriesBeforeSealClone` | `Aggregate Input Limits Reject Top Level Entries Before Seal Clone`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestAggregateInputLimitsRejectTopLevelEntriesBeforeSealClone` |
| `TestAggregateInputLimitsBeforeSealClone` | `Aggregate Input Limits Before Seal Clone`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestAggregateInputLimitsBeforeSealClone` |
| `TestExecutionBudgetAggregatesAllEntryOccurrences` | `Execution Budget Aggregates All Entry Occurrences`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestExecutionBudgetAggregatesAllEntryOccurrences` |
| `TestExecutionBudgetChargesSharedTargetPerEntryButEvaluatesOnce` | `Execution Budget Charges Shared Target Per Entry But Evaluates Once`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestExecutionBudgetChargesSharedTargetPerEntryButEvaluatesOnce` |
| `TestExecutionBudgetAcceptsExactAggregateLimits` | `Execution Budget Accepts Exact Aggregate Limits`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestExecutionBudgetAcceptsExactAggregateLimits` |
| `TestAggregateCollectionLimitsRejectEmptyEntriesBeforeSealClone` | `Aggregate Collection Limits Reject Empty Entries Before Seal Clone`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestAggregateCollectionLimitsRejectEmptyEntriesBeforeSealClone` |
| `TestEntryStatusTransitionMatrix` | `Execution Status Transition Matrix`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestEntryStatusTransitionMatrix` |
| `TestEntryStatusRejectsUnknownAndTerminalTransitions` | `Execution Status Rejects Unknown And Terminal Transitions`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_test.go`](../../domain/execution/validation_test.go) · `TestEntryStatusRejectsUnknownAndTerminalTransitions` |
| `TestWorkflowSnapshotValidateViolationOrderIsDeterministic` | 重复校验同一个含重复参数名的畸形 `WorkflowSnapshot` 50 次。 | `EXECUTION_CREATE_INSTANCE_STEP_SHAPE_INVALID` 每次返回完全相同顺序的 violation 序列；顺序仅是输入的函数。 | [`domain/execution/envelope_determinism_test.go`](../../domain/execution/envelope_determinism_test.go) · `TestWorkflowSnapshotValidateViolationOrderIsDeterministic` |
| `TestWorkflowSnapshotValidateCapsViolationsAtDeterministicPrefix` | 构造 `fault.MaxViolations+8` 个各自都不合法的顶层 step。 | 返回的 violation 数量精确等于 `fault.MaxViolations`，且全部落在 `steps`/`CodeFieldInvalid`，即保留确定性的前缀。 | [`domain/execution/envelope_determinism_test.go`](../../domain/execution/envelope_determinism_test.go) · `TestWorkflowSnapshotValidateCapsViolationsAtDeterministicPrefix` |
| `TestValidateEnvironmentSnapshotViolationOrderIsDeterministic` | 重复校验同一个含多个非法环境变量名的 `EnvironmentSnapshot` 50 次。 | `EXECUTION_ENVIRONMENT_SNAPSHOT_INVALID` 每次返回完全相同顺序的 violation 序列；变量按键排序遍历，顺序仅是输入的函数。 | [`domain/execution/envelope_determinism_test.go`](../../domain/execution/envelope_determinism_test.go) · `TestValidateEnvironmentSnapshotViolationOrderIsDeterministic` |
| `TestValidateEnvironmentSnapshotCapsViolationsAtDeterministicPrefix` | 构造 `fault.MaxViolations+8` 个各自都不合法的环境变量名。 | 返回的 violation 数量精确等于 `fault.MaxViolations`，且全部落在 `environment.variables`/`CodeFieldInvalid`，即保留排序后的确定性前缀。 | [`domain/execution/envelope_determinism_test.go`](../../domain/execution/envelope_determinism_test.go) · `TestValidateEnvironmentSnapshotCapsViolationsAtDeterministicPrefix` |
| `TestExecutionIdentityValuesRejectBlankAndOversizedValues` | `Execution Identity Values Reject Blank And Oversized Values`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/coordinate_test.go`](../../domain/execution/coordinate_test.go) · `TestExecutionIdentityValuesRejectBlankAndOversizedValues` |
| `TestInvocationPathConstructorsPreserveCanonicalEncoding` | `Invocation Path Constructors Preserve Canonical Encoding`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/coordinate_test.go`](../../domain/execution/coordinate_test.go) · `TestInvocationPathConstructorsPreserveCanonicalEncoding` |
| `TestExecutionIdentityValuesRoundTrip` | `Execution Identity Values Round Trip`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/coordinate_test.go`](../../domain/execution/coordinate_test.go) · `TestExecutionIdentityValuesRoundTrip` |
| `TestPlanFailurePolicyReturnsSealedPolicy` | `Plan Failure Policy Returns Sealed Policy`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/public_methods_coverage_test.go`](../../domain/execution/public_methods_coverage_test.go) · `TestPlanFailurePolicyReturnsSealedPolicy` |
| `TestInstanceSnapshotInvocationFindsAndOwnsValues` | `Instance Snapshot Invocation Finds And Owns Values`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/public_methods_coverage_test.go`](../../domain/execution/public_methods_coverage_test.go) · `TestInstanceSnapshotInvocationFindsAndOwnsValues` |
| `TestReferenceValidateConfigurationMatrix` | `Reference Validate Configuration Matrix`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/public_methods_coverage_test.go`](../../domain/execution/public_methods_coverage_test.go) · `TestReferenceValidateConfigurationMatrix` |
| `TestValidationValidateBoundaryAndKindMatrix` | `Validation Validate Boundary And Kind Matrix`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/public_methods_coverage_test.go`](../../domain/execution/public_methods_coverage_test.go) · `TestValidationValidateBoundaryAndKindMatrix` |
| `TestValidationGroupValidateScenarioMatrix` | `Validation Group Validate Scenario Matrix`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/public_methods_coverage_test.go`](../../domain/execution/public_methods_coverage_test.go) · `TestValidationGroupValidateScenarioMatrix` |
| `TestParameterValidateSelectAndNonSelectBoundaryMatrix` | `Parameter Validate Select And Non Select Boundary Matrix`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/public_validation_matrix_test.go`](../../domain/execution/public_validation_matrix_test.go) · `TestParameterValidateSelectAndNonSelectBoundaryMatrix` |
| `TestDraftValidatePublicRuleMatrix` | `Draft Validate Public Rule Matrix`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/public_validation_matrix_test.go`](../../domain/execution/public_validation_matrix_test.go) · `TestDraftValidatePublicRuleMatrix` |
| `TestSealCanonicalizesMultipleEntryOrder` | `Seal Canonicalizes Multiple Entry Order`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/public_validation_matrix_test.go`](../../domain/execution/public_validation_matrix_test.go) · `TestSealCanonicalizesMultipleEntryOrder` |
| `TestWorkflowSnapshotValidateRejectsMissingSteps` | `Workflow Snapshot Validate Rejects Missing Steps`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/public_validation_matrix_test.go`](../../domain/execution/public_validation_matrix_test.go) · `TestWorkflowSnapshotValidateRejectsMissingSteps` |
| `TestInstanceConstructionAndTransitionPublicErrorBoundaries` | `Instance Construction And Transition Public Error Boundaries`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/public_validation_matrix_test.go`](../../domain/execution/public_validation_matrix_test.go) · `TestInstanceConstructionAndTransitionPublicErrorBoundaries` |
| `TestInstanceStatusTransitionMatrix` | `Instance Status Transition Matrix`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/state_matrix_test.go`](../../domain/execution/state_matrix_test.go) · `TestInstanceStatusTransitionMatrix` |
| `TestEntryStatusTransitionAndTerminalMatrices` | `Entry Status Transition And Terminal Matrices`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/state_matrix_test.go`](../../domain/execution/state_matrix_test.go) · `TestEntryStatusTransitionAndTerminalMatrices` |
| `TestWorkerFenceBoundaryAndStaleErrorContract` | `Worker Fence Boundary And Stale Error Contract`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/state_matrix_test.go`](../../domain/execution/state_matrix_test.go) · `TestWorkerFenceBoundaryAndStaleErrorContract` |
| `TestValidateInstanceAcceptsEveryLegalLifecycleShape` | `Validate Instance Accepts Every Legal Lifecycle Shape`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/state_matrix_test.go`](../../domain/execution/state_matrix_test.go) · `TestValidateInstanceAcceptsEveryLegalLifecycleShape` |
| `TestValidateInstanceRejectsSingleFactorBoundaryViolations` | `Validate Instance Rejects Single Factor Boundary Violations`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/state_matrix_test.go`](../../domain/execution/state_matrix_test.go) · `TestValidateInstanceRejectsSingleFactorBoundaryViolations` |
| `TestValidateSnapshotValuesReportsTheSameUnknownKeyEveryRun` | `Validate Snapshot Values Reports The Same Unknown Key Every Run`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_determinism_test.go`](../../domain/execution/validation_determinism_test.go) · `TestValidateSnapshotValuesReportsTheSameUnknownKeyEveryRun` |
| `TestValidateBindingsReportsTheSameOffendingBindingEveryRun` | `Validate Bindings Reports The Same Offending Binding Every Run`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_determinism_test.go`](../../domain/execution/validation_determinism_test.go) · `TestValidateBindingsReportsTheSameOffendingBindingEveryRun` |
| `TestValidateBindingsPicksTheSameFailureKindEveryRun` | `Validate Bindings Picks The Same Failure Kind Every Run`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/validation_determinism_test.go`](../../domain/execution/validation_determinism_test.go) · `TestValidateBindingsPicksTheSameFailureKindEveryRun` |
| `TestWorkerFenceInvalidErrorCarriesCode` | `Worker Fence Invalid Error Carries Code`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/worker_fence_fault_test.go`](../../domain/execution/worker_fence_fault_test.go) · `TestWorkerFenceInvalidErrorCarriesCode` |
| `TestWorkerFenceInvalidIsDistinctFromStale` | `Worker Fence Invalid Is Distinct From Stale`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/worker_fence_fault_test.go`](../../domain/execution/worker_fence_fault_test.go) · `TestWorkerFenceInvalidIsDistinctFromStale` |
| `TestWorkerFenceValidReturnsNil` | `Worker Fence Valid Returns Nil`；表驱动子案例（如存在）覆盖该函数中声明的输入、状态与边界。 | 由测试断言验证返回值、错误分类、状态变更、所有权或副作用。 | [`domain/execution/worker_fence_fault_test.go`](../../domain/execution/worker_fence_fault_test.go) · `TestWorkerFenceValidReturnsNil` |

## Cross-cutting / Conformance Cases

同包及其子目录中名称含 `Conformance`、`Transaction`、`Race`、`Rollback`、`Replay`、`Concurrent` 或 `Fence` 的测试，属于跨入口契约；它们已在上方矩阵逐行列出。application 包的 `conformancetest/` 证据也归属此表。

## 维护规则

1. 新增或删除 `Test…` 函数时，必须同步更新本表；表驱动新增子案例要更新相应行的边界描述。
2. 新增公开 domain API 或 application use case 时，必须先添加公开入口清单行和至少一条可执行测试证据。
3. 文档不替代测试；冲突时以 Go 测试断言和领域契约为准。

