# 贡献指南

感谢你对 `healix-core` 的关注和贡献。该仓库是面向浏览器自动化产品的 Go 领域与执行内核；提交应保持 Core 与宿主基础设施之间既有的边界。

## 提交前准备

- 先通过 Issue 或 Pull Request 描述问题、预期行为与边界影响；较大的设计变更请先讨论方案。
- 保持改动聚焦，避免把无关重构、格式化或生成文件混入同一个提交。
- 不要提交密钥、令牌、密码、真实用户数据、私有环境配置或其他敏感信息。

## 开发与验证

请遵循仓库的现有分层、命名和错误处理约定。新增或修改行为时，应补充覆盖该行为的测试。

提交 Pull Request 前至少运行：

```bash
make check
```

该命令会执行格式检查、静态检查、测试、竞态检测、覆盖率门槛、构建和 lint。若环境不具备完整工具链，请在 Pull Request 中说明未执行的检查及原因。

提交信息使用 Conventional Commits 风格：

```text
<type>: <description>
```

常用 `type` 包括 `feat`、`fix`、`refactor`、`docs`、`test`、`chore`、`perf` 和 `ci`。

## Pull Request

请在 Pull Request 中清楚说明：

- 变更解决的问题及其边界；
- 对公开契约、兼容性或宿主适配器义务的影响；
- 已执行的验证命令和结果；
- 尚未验证的内容或已知限制（如有）。

维护者会重点审查领域不变量、公共 API、错误分类、测试覆盖和文档是否保持一致。

## 贡献许可

本项目采用 [Apache License 2.0](LICENSE)（SPDX: `Apache-2.0`）。除非你在贡献时以书面形式明确标注其他条款，向本仓库提交并被接纳的贡献均依照该许可证的条款提供。详情见 [LICENSE](LICENSE) 与 [NOTICE](NOTICE)。
